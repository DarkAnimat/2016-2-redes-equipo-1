from numpy import sin, cos , pi, linspace,arange
import fft as ffT
from scipy.io import wavfile

spm = Ac*cos(3 * (pi/7)*t + 1 * integrate.quad(lambda x: audioData.any(), 0, t))
ffT.fftGraphic(sampleFreq, spm)
